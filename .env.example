# SkyFleet Technologies — .env.example
# Copy this file to .env and fill in your actual values.
# Never commit .env to version control.

# ── SMTP settings ──
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tankuday9059@gmail.com
SMTP_PASS=your_16_char_app_password   # Gmail: myaccount.google.com/apppasswords

# ── Where contact form emails should be delivered ──
MAIL_TO=tankuday9059@gmail.com

# ── Server port (default 3001) ──
PORT=3001
